# utils/__init__.py
# Absolute imports (better choice)
# Specify the absolute path of the imported module from the project root (or any other dir which sys.path has access to).
from utils.length import get_length
from utils.lower import to_lower
from utils.upper import to_upper
